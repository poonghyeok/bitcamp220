1. 일단 서블릿 하나 추가해야됨 > command properties에 추가
2. 모델 class 추가 BoardReplyFormService
	2-1. 뷰는 인덱스가 되겠고 display div 영역에 답글 폼 띄워야됨
	- seq랑 pg알려줘야됨
		- seq랑 pg는 detail page에서 수정버튼 눌렀을 때 post영역으로 보내줌
			- seq와 updatePg 라는 객체에 담겨있다. 
			- setAttribute에 실어서 index로 포워딩하는데 idnex에는 replyform 이 포함되어있다. 같은 request객체 

3. boardReplyForm.jsp 추가
	- 형태는 write form이랑 거의 비슷
	
4 boardReply 서블릿 태워야됨.
	- 서블릿에서 가져다가 쓸 parameter가 있어야한다. pseq랑 page
	- 이걸 form jsp에서 넘겨줘야하는데 from jsp는 누구로부터 받아왔는가 이전 서블릿에서 
	- 특정 글에서 답글 작성하기를 눌렀으므로 일단 reply form에 seq가 전달이 됐다.
		- form에서 또 서블릿으로 seq를 던져준다. 
5. myBatis 설정
	 ??? 글순서를 없애고 1차 정렬을 ref로 하고 2차정렬을 lev로 하고 3차 정렬을 date로 하면안되나 ???
	- 답글 작성하히 button 눌렀을 때 모델 클래스에서 boardReply를 호출하게 되고 boardReply()는
	- 일단 list받아와서 띄울 때, 1차 정렬기준 ref, 2차 정렬기준 step 
	총 3가지의 작업을 거치게 된다. 
	1.  같은 원글을 가지고 있고 기존에 있던 애들의 step을 1씩 늘려준다. 
	2.  insert
		- ref, lev, step, pseq 는>
		- ref는 원글의 ref 들어가고, lev는 원글의 lev + 1, step은 어떻게 넣어주지 step.. 
		- 원글의 reply라는 답글수 컬럼값을 1증가시켜준다.
	3 . 

6. 리디렉션 설정
	원글의 page를 보여주자 
	- 일단 reply작성에 성공했다면 메소드 리턴값으로 수정된 행의 개수를 리턴해주자 이걸 req에 실어줘서 jsp에서 적당히 결과 띄울슁ㅆ게
	-

7. 답글이라면 들여쓰기를 설정하자 .
	- taglib을 사용하겠지
	- row는 board에서 하나의 튜플이니깐 lev 당연히 있을 것이고
	+ gif 넣어주기
8.디버깅
	8-1 답글의 답글을 달았을 때 lev이 안쌓인다. 
		- lev얹어주는 과정을 체크하러 가보자.
		- dao에서 boardReply method에서 map에 lev를 넣어주는데..
		 	boardDTO 의 객체 pdto를 하나 받아와서 pseq로 셀렉을 해서 하나 받아온다.
			pseq는 어디서 부터왔는가. map에 달려서 왔다. 

		- 처음에 list 요청할 때 pg=1로 boardList  서블릿을태우고 list가 나오고 
l		list에서 글제목을 클릭했을 때, seq에 맞는 글을 받아와서 보여주겠지 그럼 list에 문제가 있는거같은데 
		- 반복문이 돌아기 때문에 하나로 통일이 된다. 
### list 제목 클릭해서 post방식으로 detail 돌리는거 seq가 게속 한번호로만 넘어가서 get 방식으로 돌려놓음 해결할것 ####
!!@@git hub 에 버젼관리 하기 노브랜치로 @@!!

(option 마지막 정렬을 시간 기준으로 해서 .. 도전해보기)

@@ mapper완성, dao완성, 답글 작성시 출력확인@@

@@ 답글에 gif 추가하기 @@
@@ level에 맞게 들여쓰기 적용@@
	&emsp; 적용, 원글은 tab안먹게 

@@ 과제 @@
	삭제기능구현
	밀어뒀던거 다시 다 돌려놓진 않고 앞에 [원글이 삭제 된 답글입니다] 라고 입력해줄것 
	그렇게 어렵다고?
	UP UP  DEL 과정을 거쳐야한다. 

++
	페이징처리, 조회수(새로고침되어도 증가하지 않게, 세션이나 쿠키 이용 )
	
